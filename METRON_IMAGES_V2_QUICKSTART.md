# 🚀 Быстрый старт: Кэширование изображений Metron (v2)

## Что изменилось?

**Проблемы v1:**
- ❌ JSON файл с десятками тысяч записей будет тяжелым
- ❌ Ограничения API Metron (30/мин, 10000/день) не позволяют прямые запросы

**Решение v2:**
- ✅ Локальное кэширование изображений
- ✅ Легкий индекс (только список ID) - ~500KB для 100,000 записей
- ✅ Быстрый доступ O(1) через Set
- ✅ Учет лимитов API при загрузке

## Как это работает?

1. **Изображения хранятся локально** в `public/images/metron/{size}/{comicvineId}.jpg`
2. **Индекс хранит только список ID** в `data/metron-cache-index.json`
3. **Автоматическая подмена**: `getImageUrl` использует кэш, если есть

## Быстрое использование

### 1. Загрузка изображений

```bash
# Загрузить для конкретных ComicVine ID
npx tsx scripts/download-metron-images.ts --ids "4050-12345,4050-67890"

# Загрузить для 100 популярных комиксов (из БД)
npx tsx scripts/download-metron-images.ts --popular 100
```

**Важно:**
- Скрипт автоматически учитывает лимиты API (2.5 сек между запросами)
- Загружает все размеры: thumb, tiny, small, super
- Обновляет индекс автоматически

### 2. Использование в коде

Просто передайте `comicvineId` в `getImageUrl`:

```typescript
// Автоматически использует кэш, если есть
const url = getImageUrl(comic.thumb, comic.comicvine, 'thumb')
```

Если кэш есть → `/images/metron/thumb/4050-12345.jpg`  
Если кэша нет → оригинальный ComicVine URL

## Структура файлов

```
public/
  images/
    metron/
      thumb/4050-12345.jpg
      tiny/4050-12345.jpg
      small/4050-12345.jpg
      super/4050-12345.jpg

data/
  metron-cache-index.json  # {"cachedIds": ["4050-12345", ...]}
```

## Лимиты API

- **30 запросов/мин** = задержка 2.5 сек между запросами
- **10000 запросов/день** = максимум ~9000 в день (с запасом)

**Примерное время загрузки:**
- 100 комиксов = ~4 минуты
- 1000 комиксов = ~42 минуты
- 10000 комиксов = ~7 часов (но лимит 10000/день!)

## Производительность

### Размеры

- **Индекс**: ~500KB для 100,000 ID (только список строк)
- **Изображения**: ~50-100MB для 1000 комиксов (4 размера × ~25KB)
- **Загрузка индекса**: <10ms

### Скорость

- **Проверка наличия кэша**: O(1) через Set
- **Генерация URL**: O(1) - просто конкатенация
- **Нет запросов к API**: Все локально

## Рекомендации

1. **Начните с популярных**: Загрузите сначала 100-200 самых популярных комиксов
2. **Постепенная загрузка**: По 100-200 комиксов в день
3. **Мониторинг**: Следите за использованием лимитов API
4. **Git**: Изображения в `.gitignore`, индекс можно коммитить

## Проверка работы

```typescript
import { hasMetronCache, getCacheStats } from '@/lib/metron-images-v2'

// Проверка наличия кэша
if (hasMetronCache(4050-12345)) {
  console.log('Кэш есть!')
}

// Статистика
const stats = getCacheStats()
console.log(`Всего в кэше: ${stats.totalCached}`)
```

## Миграция с v1

Если вы использовали v1:
1. Удалите `data/metron-images.json` (старый формат)
2. Используйте новый скрипт для загрузки изображений
3. Код автоматически переключится на новую систему

## Документация

Полная документация: [docs/METRON_IMAGES_V2.md](docs/METRON_IMAGES_V2.md)

