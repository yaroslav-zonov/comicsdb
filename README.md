# ComicsDB - База русских переводов комиксов

Современная реализация сайта [comicsdb.ru](https://comicsdb.ru) на новых технологиях.

## 🚀 Быстрый старт

### Автоматическая установка (macOS)

```bash
# 1. Установить MySQL
npm run install:mysql

# 2. Настроить БД и импортировать дамп
npm run setup:mysql

# 3. Запустить приложение
npm run dev
```

### Ручная установка

1. Установите MySQL и создайте базу данных `comicsdb`
2. Создайте файл `.env` на основе `.env.example` с правильными учетными данными
3. Импортируйте дамп из `dump/dump-comicsdb-202512092242.sql`
4. Запустите `npm run dev`

## 📋 Требования

- Node.js 18+
- MySQL 8.0+ (или MariaDB 10.5+)
- npm или yarn

## 🛠️ Технологический стек

### Frontend & Backend
- **Next.js 14+** (App Router) - React фреймворк с SSR/SSG
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация
- **React Query** - управление состоянием и кэширование

### База данных
- **MySQL** - реляционная БД
- **Prisma ORM** - работа с БД, миграции, типизация

## 📁 Структура проекта

```
comicsdb/
├── app/                    # Next.js App Router
│   ├── api/               # API routes
│   ├── (pages)/          # Страницы сайта
│   └── layout.tsx        # Root layout
├── components/           # React компоненты
├── lib/                  # Утилиты и конфигурация
│   ├── prisma.ts        # Prisma client
│   └── utils.ts         # Вспомогательные функции
├── prisma/              # Prisma схема и миграции
│   └── schema.prisma
├── scripts/             # Скрипты установки и миграции
└── dump/                # MySQL дамп
```

## 🔧 Основные команды

```bash
# Разработка
npm run dev              # Запуск dev сервера

# База данных
npm run db:studio        # Prisma Studio (GUI для БД)
npm run db:pull          # Синхронизация схемы с БД
npm run db:generate      # Генерация Prisma Client

# Установка
npm run install:mysql    # Установка MySQL (macOS)
npm run setup:mysql      # Настройка БД и импорт дампа

# Production
npm run build            # Сборка для production
npm start                # Запуск production сервера
```

## 📚 Документация

- [DOCUMENTATION.md](./DOCUMENTATION.md) - Полная документация проекта
- [FIELDS_CONFIGURATION.md](./FIELDS_CONFIGURATION.md) - Конфигурация полей для карточек и таблиц
- [SEARCH_DOCUMENTATION.md](./SEARCH_DOCUMENTATION.md) - Документация поискового механизма

## 🌐 API Endpoints

- `GET /api/comics` - Список комиксов
- `GET /api/series` - Список серий
- `GET /api/publishers` - Список издательств
- `GET /api/health` - Проверка состояния БД

## 📊 Статистика

- **Всего комиксов в базе:** 56,538
- **Издательства:** Marvel, DC Comics, Image и др.
- **Сайты переводчиков:** 50+

## 🔐 Переменные окружения

### Локальная разработка

Создайте `.env` файл на основе `.env.example`:

```env
DATABASE_URL="mysql://user:password@localhost:3306/comicsdb"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

### Деплой на Vercel

1. Подключите репозиторий GitHub к Vercel
2. Добавьте переменную окружения в настройках проекта:
   - **Key:** `DATABASE_URL`
   - **Value:** `mysql://yazonov:пароль@95.213.203.112:3306/comicsdb`
   - **Важно:** Если в пароле есть спецсимволы, закодируйте их в URL-формате:
     - `(` → `%28`
     - `)` → `%29`
     - `*` → `%2A`
     - `[` → `%5B`
     - `]` → `%5D`
   - Выберите окружения: **Production**, **Preview**, **Development**
3. Vercel автоматически запустит сборку при следующем коммите

**Примечание:** Проект настроен для автоматической работы с внешней БД на Vercel. Prisma Client генерируется автоматически при сборке (`postinstall` и `build` скрипты).

## 🐛 Решение проблем

### MySQL не запускается

**macOS:**
```bash
brew services restart mysql
```

**Linux:**
```bash
sudo systemctl restart mysql
```

### Ошибка подключения к БД

1. Проверьте, что MySQL запущен
2. Проверьте учетные данные в `.env`
3. Проверьте подключение: `npx prisma db pull`

### Изображения не загружаются

Некоторые изображения с Comicvine могут быть недоступны из-за защиты Cloudflare:

**Решения:**
1. Использовать внешний CDN сервис (Cloudinary, imgix)
2. Кэшировать изображения локально
3. Использовать прокси-сервер (например, Cloudflare Workers)

**Текущая конфигурация:**
- Изображения загружаются напрямую с Comicvine
- Включена оптимизация Next.js Image
- Кэширование в браузере на 30 дней

### Проверка состояния

```bash
# Проверка API
curl http://localhost:3000/api/health

# Просмотр данных
npx prisma studio
```

## 📝 Лицензия

Проект создан для каталогизации русских переводов комиксов.

## 🤝 Вклад

Для вопросов и предложений создайте issue в репозитории.

---

**ComicsDB** - База русских переводов комиксов © 2024
