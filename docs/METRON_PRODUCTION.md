# 🚀 Metron на Production (Vercel)

## Как работает кэш на Vercel

### Проблема
На Vercel файловая система **read-only**, поэтому файл `data/metron-cache-index.json` не может быть записан.

### Решение
Система автоматически определяет окружение и использует:

1. **На Vercel (production):**
   - ✅ In-memory кэш (работает в памяти)
   - ✅ Загружает начальные данные из файла при старте
   - ⚠️ Кэш теряется при перезапуске сервера (это нормально)

2. **На локальной разработке:**
   - ✅ In-memory кэш
   - ✅ Сохранение в файл `data/metron-cache-index.json`
   - ✅ Кэш сохраняется между перезапусками

## Что это означает

### На Vercel
- Кэш работает **в памяти** во время работы сервера
- При первом запросе к комиксу - проверка Metron API
- Результат сохраняется в памяти
- При следующем запросе - используется кэш из памяти
- При перезапуске сервера - кэш сбрасывается, но это нормально

### Преимущества
- ✅ Не требует внешних сервисов
- ✅ Работает "из коробки"
- ✅ Быстрый доступ (O(1) в памяти)
- ✅ Не тратит запросы к Metron API повторно

### Недостатки
- ⚠️ Кэш теряется при перезапуске (но это редко на Vercel)
- ⚠️ Каждый инстанс сервера имеет свой кэш (но это нормально)

## Мониторинг

### Проверка работы на Vercel

1. **Через API endpoint:**
```
https://your-app.vercel.app/api/metron/status
```

2. **Через логи:**
В Vercel Dashboard → Functions → Logs
Ищите сообщения:
```
🔍 [Metron] Проверяю ComicVine ID: ...
✅ [Metron] Найдено изображение для ...
```

3. **Через Network tab:**
Откройте страницу комикса и проверьте:
- Запросы к `metron.cloud/api/v1/issue/` (только при первом обращении)
- Использование Metron URL для изображений

## Оптимизация (опционально)

### Вариант 1: Vercel KV (Redis)

Если нужен персистентный кэш между перезапусками:

1. Установите Vercel KV:
```bash
vercel kv create
```

2. Обновите код для использования KV вместо файла

3. Преимущества:
   - ✅ Кэш сохраняется между перезапусками
   - ✅ Общий кэш для всех инстансов
   - ✅ Персистентное хранилище

### Вариант 2: База данных

Можно хранить кэш в БД, но:
- ⚠️ У вас нет прав на запись в БД
- ⚠️ Нужны дополнительные запросы к БД

## Рекомендации

### Для текущей реализации (in-memory)

1. **Это нормально** - кэш работает эффективно
2. **Популярные комиксы** будут кэшироваться быстро
3. **Редкие комиксы** проверяются при первом обращении
4. **Лимиты Metron** соблюдаются автоматически

### Если нужен персистентный кэш

Используйте Vercel KV или внешний Redis:
- Upstash Redis (бесплатный тариф)
- Vercel KV (интеграция с Vercel)

## Проверка работы

### На Vercel

1. Откройте страницу комикса со старым форматом
2. Проверьте Network tab - должен быть запрос к Metron API
3. Обновите страницу - запроса к Metron не должно быть (используется кэш)
4. Проверьте API endpoint `/api/metron/status` - должен показать статистику

### Локально

1. Откройте страницу комикса
2. Проверьте файл `data/metron-cache-index.json` - должен обновиться
3. Запустите скрипт `npx tsx scripts/check-metron-status.ts`

## Итого

✅ **Система работает на Vercel** - использует in-memory кэш  
✅ **Не требует дополнительных сервисов** - работает "из коробки"  
✅ **Эффективно** - избегает повторных запросов к Metron API  
⚠️ **Кэш теряется при перезапуске** - но это нормально и не критично

