# 🔍 Как работает система Metron - простое объяснение

## Главное

**JSON файл на GitHub - это НЕ хранилище данных!**

Это просто начальный файл с пустыми данными. Реальная работа происходит **в памяти сервера**.

## Как это работает на самом деле

### 1. Пользователь открывает страницу комикса

```
Пользователь → Открывает /publishers/123/456/789
```

### 2. Сервер проверяет формат изображения

```typescript
// В коде страницы комикса
const thumb = await getImageUrlWithMetron(comic.thumb, comic.comicvine, 'thumb')
```

### 3. Если старый формат (`api/image/`) - проверяем Metron

```
Сервер → Проверяет: есть ли в памяти?
  ├─ Есть → Использует Metron URL ✅
  └─ Нет → Запрос к Metron API → Сохраняет в память → Использует
```

### 4. Результат сохраняется В ПАМЯТИ сервера

```
В памяти сервера (не в файле!):
{
  "4050-12345": "https://static.metron.cloud/...",
  "4050-67890": "https://static.metron.cloud/..."
}
```

## Что происходит с JSON файлом?

### На GitHub
- ✅ Файл есть, но он **пустой** (или с начальными данными)
- ✅ Он **НЕ обновляется** автоматически
- ✅ Это нормально - он не используется на production

### На локальной разработке
- ✅ Файл **обновляется** при работе
- ✅ Можно посмотреть результаты локально
- ✅ Но на Vercel файл не используется

### На Vercel (production)
- ✅ Файл **НЕ обновляется** (read-only файловая система)
- ✅ Все работает **в памяти сервера**
- ✅ Это нормально и правильно

## Про скрипт фоновой обработки

### Что это?
Скрипт `scripts/process-metron-images-background.ts` - это **отдельный инструмент**, который нужно запускать **вручную**.

### Как он работает?

```bash
# Запускаете вручную на своем компьютере
npx tsx scripts/process-metron-images-background.ts
```

**Что он делает:**
1. Находит все комиксы со старым форматом в БД
2. По очереди проверяет их в Metron API
3. Сохраняет результаты в файл `data/metron-cache-index.json`
4. Соблюдает лимиты (4 сек между запросами)

### Зачем он нужен?
- Чтобы **заранее** обработать все комиксы
- Чтобы не ждать, пока пользователи откроют страницы
- Чтобы заполнить файл для локальной разработки

### Он НЕ запускается автоматически!
- ❌ Не запускается на Vercel
- ❌ Не запускается при деплое
- ❌ Не работает в фоне сам по себе
- ✅ Нужно запускать **вручную** на своем компьютере

## Итого - как это работает

### Автоматически (при обращении пользователей)

1. Пользователь открывает страницу комикса
2. Сервер проверяет: старый формат изображения?
3. Если да → проверяет Metron API
4. Сохраняет результат **в памяти сервера**
5. При следующем запросе → использует кэш из памяти

**Где хранится:** В памяти сервера (не в файле!)

### Вручную (скрипт)

1. Вы запускаете скрипт на своем компьютере
2. Скрипт обрабатывает все комиксы
3. Сохраняет результаты в файл
4. Файл можно закоммитить в git (но это не обязательно)

**Где хранится:** В файле `data/metron-cache-index.json` (только локально)

## Вопросы и ответы

**Q: Почему JSON файл пустой на GitHub?**  
A: Потому что на Vercel он не используется. Все работает в памяти.

**Q: Как тогда данные сохраняются?**  
A: В памяти сервера. При каждом запросе проверяется кэш в памяти.

**Q: Что если сервер перезапустится?**  
A: Кэш сбросится, но это нормально. При следующем запросе снова проверит Metron.

**Q: Зачем тогда скрипт?**  
A: Чтобы заранее обработать комиксы локально и заполнить файл для разработки.

**Q: Скрипт работает в фоне?**  
A: Нет! Нужно запускать вручную. Он просто обрабатывает комиксы по очереди.

## Простое сравнение

### Как работает сейчас (автоматически)
```
Пользователь → Страница → Проверка Metron → Кэш в памяти → Готово
```

### Как работает скрипт (вручную)
```
Вы → Запускаете скрипт → Обрабатывает все → Сохраняет в файл
```

**Скрипт НЕ нужен для работы сайта!** Он только для предварительной обработки.

